import { suite } from "uvu";
import * as assert from "uvu/assert";

import { setWallet } from "../src/api/set-wallet.js";

const test = suite("set-wallet");

test.skip("set-wallet", async () => {});

test.run();
